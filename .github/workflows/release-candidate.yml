name: Release Candidate

on:
  push:
  workflow_dispatch:

jobs:
    envoirenment:
        name: Envoirenment
        timeout-minutes: 10
        runs-on: ubuntu-latest

        steps:
            - name: Setting variables
              uses: dolorestec/devops/.github/workflows/envoirenment.yml@main

