name: Build
description: "Build a docker image"

runs:
  using: composite
  steps:
    - name: Docker image build
      shell: bash
      run: |
        echo "Building docker image ${{ github.repository }}:${{ github.ref_name }}"; \
        docker build . \
        --tag ${{ github.repository }}:${{ github.ref_name }}; \
